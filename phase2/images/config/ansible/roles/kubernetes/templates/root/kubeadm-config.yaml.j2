apiVersion: kubeadm.k8s.io/v1beta4
kind: ClusterConfiguration
kubernetesVersion: {{ kubernetes_apt_version | regex_replace('-.*$', '') }}
controlPlaneEndpoint: "{{ kubernetes_control_plane_host_name }}:6443"
networking:
  podSubnet: {{ kubernetes_pod_subnet }}
